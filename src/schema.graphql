type Query {
    link(id: ID!) :Link 
    feed: [Link!]!
}

type Link{
    id: ID!
    description: String!
    url: String!
}

type Mutation {
    updateLink(id:ID!, url:String, description: String): Link 
    deleteLink(id:ID!): Link
    
}